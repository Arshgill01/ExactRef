# ExactRef agents

Read `CONTEXT.md`, `PRODUCT.md`, and `PLANS` in `docs/PACKAGE.md` before editing.

- Default path is fixture replay. Do not place a CALL-E call.
- Do not leak intended identifiers into tasks.
- `independently_verified` requires a typed value and a second-channel claim.
- Do not commit `.env`, `.data`, or secrets.
